{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://p42.xyz/schemas/deployment-role-acceptance-v2.json",
  "title": "P42 detached deployment role acceptance packet",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "policyVersion", "chainId", "requestSetNonce", "requestSetDigest", "pendingManifestBytesDigest", "manifestBindingDigest", "releaseBindingDigest", "capsuleBytesDigest", "capsuleDigest", "expectedExplorerDossierDigest", "slateDigest", "configDigest", "deploymentCommit", "timelock", "topologyDigest", "contractCount", "expiresAt", "acceptances", "packetDigest"],
  "properties": {
    "schema": { "const": "p42-prizes/deployment-role-acceptance/v2" },
    "policyVersion": { "const": "p42-governance-role-policy/v2" },
    "chainId": { "type": "integer", "minimum": 1 },
    "requestSetNonce": { "$ref": "#/$defs/bytes32" },
    "requestSetDigest": { "$ref": "#/$defs/digest" },
    "pendingManifestBytesDigest": { "$ref": "#/$defs/digest" },
    "manifestBindingDigest": { "$ref": "#/$defs/digest" },
    "releaseBindingDigest": { "$ref": "#/$defs/digest" },
    "capsuleBytesDigest": { "$ref": "#/$defs/digest" },
    "capsuleDigest": { "$ref": "#/$defs/digest" },
    "expectedExplorerDossierDigest": { "$ref": "#/$defs/digest" },
    "slateDigest": { "$ref": "#/$defs/digest" },
    "configDigest": { "$ref": "#/$defs/digest" },
    "deploymentCommit": { "type": "string", "pattern": "^[0-9a-f]{40}$" },
    "timelock": { "$ref": "#/$defs/address" },
    "topologyDigest": { "$ref": "#/$defs/digest" },
    "contractCount": { "const": 47 },
    "expiresAt": { "type": "integer", "minimum": 1 },
    "acceptances": { "type": "array", "minItems": 15, "maxItems": 15, "items": { "$ref": "#/$defs/acceptance" } },
    "packetDigest": { "$ref": "#/$defs/digest" }
  },
  "$defs": {
    "digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "address": { "type": "string", "pattern": "^0x[0-9a-fA-F]{40}$" },
    "bytes32": { "type": "string", "pattern": "^0x[0-9a-fA-F]{64}$" },
    "acceptance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["role", "address", "nonce", "riskAccepted", "roleAccepted", "signature"],
      "properties": {
        "role": { "enum": ["timelock-signer", "guardian", "treasury", "production-launch-authority", "independent-security-authority", "governance-authority", "resolver-quorum-signer"] },
        "address": { "$ref": "#/$defs/address" },
        "nonce": { "$ref": "#/$defs/bytes32" },
        "riskAccepted": { "const": true },
        "roleAccepted": { "const": true },
        "signature": { "type": "string", "pattern": "^0x[0-9a-fA-F]{130}$" }
      }
    }
  }
}
