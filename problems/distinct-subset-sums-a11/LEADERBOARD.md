# Leaderboard

This package has no on-chain submissions. Local reference row:

| solution | score | improvement | notes |
|---|---:|---:|---|
| `tests/conway-guy-594.json` | `594/1` | `0/1` | Conway-Guy-lineage witness from the OEIS A276661 comment line (`a(11) <= 594`). This witness IS the seed frontier, so it earns no improvement. `a(11)` is open; any valid witness with max element `< 594` takes the lead. Proven floor: `a(11) >= 310`. |
