# Leaderboard

This package has no on-chain submissions. Local reference row:

| solution | score | improvement | notes |
|---|---:|---:|---|
| `tests/fixtures/seed-greedy-376.json` | `376/1` | `0/1` | Greedy 9-element B3 set from OEIS A051912 (terms 0..8) shifted +1 into `{1..376}`. This witness IS the seed frontier, so it earns no improvement. True frontier is somewhere in `[151, 376]` (A387704 has `a(150) = 8`). |
