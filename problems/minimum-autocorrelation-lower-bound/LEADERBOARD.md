# Leaderboard

This package has no on-chain submissions. Local reference row:

| solution | score | improvement | notes |
|---|---:|---:|---|
| `tests/seed-b23f2d3.json` | `2378625/5958277` | `0/1` | Exact step-function lower-bound certificate converted from source commit `b23f2d3`; the frontier seed is not an improvement. |

The value of the open constant is not known. The published upper side
`C <= 411/1000` is context, not a leaderboard score or an attained optimum.
